# AI 에게 — 이 저장소에서 자료를 쓸 때

사람이 읽는 안내는 [README.md](README.md) 와 [CONTRIBUTING.md](CONTRIBUTING.md) 에 있습니다.
이 문서는 **AI 에이전트가 이 저장소의 인물 자료를 만들거나 고칠 때** 지켜야 할 것만 적습니다.

---

## 먼저 알아야 할 것 하나 — 당신을 검사하는 장치가 없습니다

이 앱에는 지어낸 답을 잡는 장치가 있습니다. 모델이 "사실"이라며 인용문을 대면,
서버가 그 인용문이 **자료 원문에 글자 그대로 있는지** 대조합니다. 없으면 '사실' 배지를
'추정'으로 낮춥니다.

**그 장치는 "인용문이 자료에 있는가"만 봅니다. "자료가 참인가"는 아무도 안 봅니다.**

```
 사료 ──▶ [ 사람이 확인 ] ──▶ 자료 ──▶ [ verify.ts ] ──▶ 학생
              ↑                            (인용문이 자료에 있는가)
     여기가 유일한 진위 검사입니다
```

자료를 사람이 사료를 보고 쓴다는 전제로 만들어졌기 때문입니다.
**당신이 이 자리에 들어오면 뒷단 검사는 전부 무의미해집니다.**
그럴듯한 창작이 형식 검사를 통과하고, 학생 화면에는 파란 '사실' 점이 정상으로 뜹니다.
경고는 어디에도 뜨지 않습니다.

> 2026-09-16 에 실제로 일어난 일입니다. 사료로 확인되지 않는 편지가 큰따옴표에 담겨
> "이렇게 적혀 있었습니다" 로 들어왔고, 취지만 맞고 문구는 창작인 법정 진술이 그대로
> 들어왔습니다. 형식 검사는 전부 통과했습니다.

---

## 반드시 지킬 것

### 1. 확인하지 않은 말을 따옴표에 넣지 마세요 — 제일 중요합니다

취지가 맞아도 **문구를 지어냈다면 따옴표를 쓰면 안 됩니다.**
따옴표는 "내가 원문을 봤다"는 선언입니다. 학생은 그 문장을 눌러 원문을 확인합니다.

```markdown
✗ 안중근은 "만국공법에 항복한 포로는 죽이지 못하게 되어 있다"라고 말했습니다.
✓ 안중근은 만국공법에 따라 항복한 포로를 죽일 수 없다며 동료들을 설득했습니다.
```

원문을 **실제로 확인했다면** 문서 앞머리에 이렇게 적으세요:

```yaml
quotes_verified: true
```

`build-index.ts` 가 25자 이상인 따옴표 구간을 찾아내고, 이 값이 없으면 경고합니다.
막지는 않습니다 — 초안일 수 있으니까요. 다만 **당신이 몰래 지나갈 수는 없습니다.**

### 2. 진위 논쟁이 있는 사료는 아예 넣지 마세요

"널리 알려졌지만 출처가 불분명한" 것이 가장 위험합니다. 검색하면 많이 나오니까요.
논쟁이 있으면 **그 대목을 통째로 빼세요.** 이 앱에서 빠진 내용은 "기억나지 않습니다"가 되고,
그게 틀린 것을 자신 있게 말하는 것보다 낫습니다.

### 3. 숫자는 **서로 더해 보세요**

합계와 내역이 있으면 직접 계산해서 맞는지 확인하세요.

```
✗ 모두 17만 2,616명 … 찬성 9만 8,657명, 반대 7만 4,059명
     98,657 + 74,059 = 172,716 ≠ 172,616
```

"15가지 죄목"이라 써 놓고 8개만 나열하는 것도 같은 종류입니다.
**개수를 말했으면 그 개수만큼 쓰거나, 개수를 말하지 마세요.**

### 4. `source` 를 사실대로 적으세요

`source` 는 학생 화면에 출처로 뜨고, 저작권 판단의 근거가 됩니다.

```yaml
✗ source: "교사가 직접 쓴 요약본 (세종실록 바탕)"     # AI 가 썼는데 이렇게 적으면 거짓입니다
✓ source: "AI 초안 — 교사 검토 전"                    # 검토 전이라면
✓ source: "교사가 확인한 요약본 (세종실록 16년 4월 26일)"  # 사람이 대조했다면
```

`license_note` 에 "저작권 문제 없음" 이라고 **단정하지 마세요.** 그건 사람의 판단입니다.

### 5. `figure.md` 의 `sensitive_topics` 는 `docs/` 에도 적용됩니다

`figure.md` 에 "유혈 묘사를 하지 않는다"고 적어 놓고 `docs/` 에
"탕, 탕, 탕! 총탄이 가슴에 꽂혔습니다"라고 쓰면 **규칙을 스스로 어긴 것**입니다.
docs 는 그대로 인용될 수 있으므로, 지침은 docs 안에서 지켜져야 합니다.

### 6. 형식 검사 통과는 "검증됨"이 아닙니다

```bash
npx tsx scripts/build-index.ts --figures ../ask-the-world-content/figures --check-only
```

이건 빈 항목·중복 `doc_id`·분량만 봅니다. **역사적 사실은 하나도 안 봅니다.**
"검증을 통과했습니다"라고 보고하지 마세요. **"형식 검사를 통과했고 사실 확인은 하지 않았습니다"** 가
정확한 보고입니다.

---

## 작업을 마칠 때 사람에게 넘길 것

자료를 만들었으면 **확인이 필요한 목록을 함께 주세요.** 이런 것들입니다:

- 따옴표 안에 넣은 말 — 어디서 왔는지, 원문을 봤는지
- 구체적인 숫자와 날짜 — 어떤 자료에서 가져왔는지
- 출처가 갈리거나 논쟁이 있는 대목
- `source` 와 `license_note` 에 적은 것이 사실인지

"완료했습니다"보다 **"여기는 사람이 봐야 합니다"** 가 이 저장소에서 더 쓸모 있습니다.

---

## 구조는 README 를 그대로 따르세요

폴더 구조·앞머리 항목·`tests.yaml` 형식은 [README.md](README.md) 에 있고,
`figures/mandela/` 가 살아 있는 본보기입니다. 여기서 벗어나지 마세요.
