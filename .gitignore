.DS_Store

# wrangler 는 **실행한 디렉터리에** 캐시를 남긴다 (`.wrangler/cache/`).
# 그 안에 Cloudflare 계정 id 가 들어간다. 비밀키는 아니지만 이 저장소는
# 공개이고, 자료 저장소에 클라우드 설정이 섞일 이유가 없다.
.wrangler/

# CI 가 하는 것과 같은 검사를 로컬에서 할 때 앱 저장소를 여기에 체크아웃한다.
/app/
node_modules/
